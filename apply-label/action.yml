name: 'Apply label'
description: 'Apply the project label on issues and pull requests.'
inputs:
  github-token:
    description: 'GitHub token with read and write access on the project.'
    required: true
runs:
  using: "node16"
  main: "index.js"
